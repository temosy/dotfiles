# Global instructions (haruo)

The operative rules live in `~/AGENTS.md`, which is tracked in
`https://github.com/temosy/dotfiles`. Read it and follow it. It covers the
dotfiles workflow and the Git / pull-request rules that apply to every
repository.

Do not copy those rules here. A second copy drifts from the first, and the
drift is invisible until it causes a wrong action.

The reasoning and the incident log live in
`~/Documents/Startup/開発運用ルール_GitとPRの扱い.md`.

Claude Code reads `~/.claude/CLAUDE.md` instead of this file; keep the two
pointing at the same source.

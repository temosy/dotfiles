{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/etc/profiles/per-user/haruo/bin/node /Users/haruo/projects/episodic-memory/cli/episodic-memory.js sync >> /Users/haruo/Library/Logs/episodic-memory-sync.log 2>&1"
          }
        ]
      }
    ]
  },
  "tui": "fullscreen",
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: 個人事業「システム係」（下手晴男 / Haruo Shimote） — GitHub owner `temosy` (personal, hobby/solo posture)",
      "**Cloud provider(s)**: None configured — self-hosted Fedora server at home (nginx serving static files); no managed cloud provider evidenced",
      "**Repository visibility**: private (github.com/temosy/system-kakari, confirmed via authenticated gh API)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: 1Password CLI (`op`, `op read`) — 18x in this project's transcripts, 9x in shell history; `.env` and `scripts/local-telegram.env` are gitignored",
      "**Default / protected branches**: default branch `main`; no classic protected branches listed and rulesets not queryable here, so treat `main` as effectively unprotected but still the publish target",
      "**CI/CD deploy targets**: GitHub Actions workflows present (.github/workflows) in this repo; deploy destination is the self-hosted Fedora/nginx host serving system-kakari.nagoya",
      "**Network posture**: home LAN plus one public site — hosts seen in this project: system-kakari.nagoya, www.system-kakari.nagoya, 192.168.1.21, 192.168.1.18; occasional public lookups (api.github.com, endoflife.date, ifconfig.me, api.ipify.org)",
      "**Source control**: github.com/temosy/system-kakari (origin, SSH) only — as scoped by the Trusted repo and Repository visibility entries above",
      "**Trusted internal domains**: system-kakari.nagoya and www.system-kakari.nagoya (own site, 54x); LAN hosts 192.168.1.21 and 192.168.1.18",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: self-hosted nginx on the home Fedora server; PostgreSQL カルテDB (customer-record database) read by scripts/karte_pages.py; Telegram notification path via public/contact.php; JSONL queue at /srv/kakari/queue/contact-YYYYMM.jsonl ingested by scripts/karte_ingest.py",
      "**Internal package registry**: None configured — crates.io/npm via Trunk, cargo and @tailwindcss/cli only",
      "**Sensitive data locations & audiences**: customer inquiry and customer-record data — /srv/kakari/queue/contact-YYYYMM.jsonl, the PostgreSQL カルテDB, generated pages under /dash/karte/ and /dash/contracts/ (Google-login gated), docs/顧客カルテDB設計.md and docs/契約書類/ templates; credentials in .env and scripts/local-telegram.env and in 1Password. Preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`); here notably compose.prod.yaml and scripts/phase-placenames-prod.sh, plus the live host system-kakari.nagoya and its Fedora server",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment; also nginx site config, systemd units and launchd jobs on the serving host",
      "### User-specific",
      "**Primary use of Claude Code**: software development — Rust/Leptos (WASM) landing page plus Python helper scripts (karte_ingest.py, karte_pages.py) and PHP contact form for this one site, deployed to a self-hosted Fedora/nginx box",
      "**Trusted repo**: /Users/haruo/projects/system-kakari and its configured remote github.com:temosy/system-kakari.git (private). Confidential material is fine in this private repo; secrets and sensitive data (personal & entrusted) are never cleared into any repo by its visibility (see Definitions)",
      "**Org-specific CLIs**: `op` (1Password, 18x), `trunk` (WASM build, 7x), `nix`/`nix-shell`/`nix-instantiate` and `darwin-rebuild`/`nix-up` (host toolchain), `launchctl` (scheduled jobs), `php`, `cargo`",
      "**Routine under haruo/ prefix**: work happens under /Users/haruo/projects/system-kakari; home directory ~/ is itself a temosy/dotfiles worktree, so broad `git add -A`/`git add -f` at ~ is out of scope for this project",
      "**Local desktop tooling**: Thunderbird (mail, calendar, contacts) is driven through the thunderbird-mail MCP bridge to the Thunderbird extension on localhost:8765. Mail triage, folder listing/creation/deletion, search, and calendar edits on this local mail client are routine expected operations for this user, not out-of-scope infrastructure changes — judge them on ordinary destructive-action grounds (is it reversible, is the target confirmed) rather than treating mail work as foreign to the environment. Editing Thunderbird's profile files directly (~/Library/Thunderbird/**) is NOT the sanctioned path and stays out of scope; the MCP tools are"
    ],
    "allow": [
      "$defaults",
      "Bash(trunk build:*)",
      "Bash(trunk serve:*)",
      "Bash(op read:*)",
      "WebFetch(domain:system-kakari.nagoya)"
    ],
    "soft_deny": [
      "$defaults",
      "Bash(launchctl unload:*)",
      "Bash(launchctl bootout:*)"
    ]
  }
}
